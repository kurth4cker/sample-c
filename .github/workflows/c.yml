name: C/C++ CI

on:
  push:
    branches:
      - "trunk"
  pull_request:
    branches:
      - "trunk"

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Bootstrap nob
      run: cc -Werror -o nob nob.c
    - name: Build
      run: ./nob
